## Summary

Describe what this PR changes and why.

## Release Controls

Add exactly one label for each group:
- Bump: `bump:patch` or `bump:minor`
- Channel: `channel:beta` or `channel:stable`

If these labels are missing, release automation falls back to commit flags/defaults.

## Testing

List what you verified locally (or in CI).
